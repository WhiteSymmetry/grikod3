[workspace]
preview = ["pixi-build"]
authors = ["Mehmet Keçeci <mkececi@yaani.com>"]
channels = ["pypi", "https://prefix.dev/conda-forge", "conda-forge"]
platforms = ["linux-64"]
name = "grikod3"
version = "0.1.6"

[package]
name = "grikod3"
version = "0.1.6"
license = "AGPL-3.0-or-later"

[dependencies]
python = ">=3.11"
pip = ">=25.3"
build = "*"        # <--- python -m build komutunun çalışması için bu şarttır
numpy = ">=2.4.6"
cython = ">=3.2.8"
pytest = ">=9.1.1"

[tasks]
build = "python -m build"
start = "python -c 'import grikod3; print(grikod3.__version__)'"
install = "pip install -e ."
test = { depends-on = ["install"], cmd = "pytest" }

[package.build.backend]
name = "pixi-build-python"
version = "*"

# DOĞRU HOST BAĞIMLILIKLARI (hatchling silindi, setuptools ve cython eklendi)
[package.host-dependencies]
python = ">=3.11"
setuptools = ">=61.0"
cython = ">=3.0.0"
pip = "*"
