name: Publish to PyPI (manylinux uyumlu)

on:
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'

      - name: Build wheels with cibuildwheel
        uses: pypa/cibuildwheel@v4.1.0  # 🆕 En son sürüm
        env:
          # 3.11'den 3.15'e kadar tüm sürümler
          CIBW_BUILD: "cp311-* cp312-* cp313-* cp314-* cp315-*"
          # Python 3.15 için opt-in gerekebilir (dökümana göre)
          CIBW_ENABLE: "cpython-prerelease"
          CIBW_ARCHS: "auto64"
          CIBW_MANYLINUX_X86_64_IMAGE: "manylinux_2_28"

      - name: Upload wheels as artifacts
        uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

  publish:
    name: Publish to PyPI
    needs: build_wheels
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download all wheel artifacts
        uses: actions/download-artifact@v7
        with:
          path: dist

      - name: Move wheels to root dist directory
        run: |
          find dist -type f -name "*.whl" -exec mv -v {} dist/ \;
          # Boş klasörleri temizle
          find dist -type d -empty -delete
          ls -la dist/
        shell: bash

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.14.0
        with:
          packages-dir: dist/
